<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Information Card</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
    <a class="text-2xl text-blue-700" href="/">Create User</a>
<div class="bg-gray-100 flex justify-center items-center h-screen w-full gap-5 flex-wrap">
  <!-- User Information Card -->
  <% user.forEach((val)=>{%>
    <div class="bg-white p-8 rounded-lg shadow-lg  flex  ">
       
      <div >
        
        <!-- User Name -->
        <div class="mb-4">
          <h3 class="text-lg font-medium text-gray-700" >Name:</h3>
          <p class="text-gray-600"><%= val.name %></p> <!-- You can replace with EJS variable if dynamic -->
        </div>
    
        <!-- User Email -->
        <div class="mb-4">
          <h3 class="text-lg font-medium text-gray-700">Email:</h3>
          <p class="text-gray-600"><%= val.email %></p> <!-- You can replace with EJS variable if dynamic -->
        </div>
    
        <!-- User Password -->
        <div class="mb-6">
          <h3 class="text-lg font-medium text-gray-700">Password:</h3>
          <p class="text-gray-600">*******</p> <!-- Ideally, don't display passwords in plaintext! -->
        </div>

        
    
        <!-- Button (Optional) -->
        <div class="flex justify-center gap-5">
          <a href="/edit/<%= val._id %>">
            <button class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                Edit 
              </button>
          </a>
          <a href="/delete/<%= val._id %>">
            <button class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
                Delete 
              </button>
          </a>
        </div>
      </div>
      <div>

      </div>

      <div class="mb-7">
        
        <img class="w-[250px] h-[250px] rounded ml-[20px] object-cover" src=<%= val.url %> alt="wrong"> <!-- Ideally, don't display passwords in plaintext! -->
    </div> 
       
    </div>
<%}) %>

</div>

</body>
</html>



      